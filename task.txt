29-07-2025:

why int main in cpp and not void??

create a class named Product 
declare data members:
id, name, price, quantity
int  char  float  int

if user is insrting price or quantity in negative then display error message


get data from user in main function
and after that display the data in main function



create 4 functions in class named :
add,sub,mul,div 
eg int add (no1,no2){

        return no1 + no2;
}


main(){


    //menu using switvvch case

    enter 1 for add... 2...3...4..

    switch(choice){

        case 1:
            calc.add(100,20);
        break;
    }

}

multilevel : --> a -->param(2 param)-->b(1 param)-->c()defult->


&& operaotr overload --> 2 user age same ....

📌 10 Practice Problems (Easy → Medium)
🔹 Easy

Reverse a String (using at())

Input a string and print its reverse by accessing characters with at().

Check Palindrome (using front() and back())

Check if a word reads the same forwards and backwards.

Use front() and back() in each iteration.

Count Vowels (using size() and at())

Count how many vowels (a,e,i,o,u) are in the string.

🔹 Medium

Capitalize Middle Character (using at())
If the string length is odd, convert the middle character to uppercase.

Example: "hello" → "heLlo".

Insert Separator (using insert())

Input: "abcdef", Output: "a-b-c-d-e-f"

Insert "-" between every two characters.

Remove Extra Spaces (using erase())

Input: "I love C++"

Output: "I love C++"

Erase consecutive spaces until only one remains.

Replace Word (using replace())

Input: "I like Java"

Replace "Java" with "C++".

🔹 Challenging Medium

Trim String (using erase() and size())

Input: " hello world "

Remove leading and trailing spaces manually.

Dynamic Buffer Test (using capacity())

Start with an empty string. Keep appending characters (append() or push_back()) one by one.

Print size() and capacity() after each step to observe capacity doubling behavior.

Clear After Use

Input a sentence. Process it (for example: count vowels).

Then call clear() and confirm by checking empty().

